# Copy this file to a local ignored env file outside the public repository.
# A real value must never be committed or put in a worker command line.
MACHINE_MANAGER_GITHUB_TOKEN=github_pat_replace_locally
